# Leave unset in production — the app calls same-origin /api/* by default,
# which frontend/api/[...path].js proxies to the backend server-to-server.
# Only set this to bypass the proxy and call a backend directly.
VITE_API_URL=
